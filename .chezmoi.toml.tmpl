[git]
    autoCommit = true
    autoPush = {{ if (stat (joinPath .chezmoi.homeDir ".is_work")) }}false{{ else }}true{{ end }}

[edit]
    apply = true

[template]
  options = ["missingkey=zero"]

[data]
  isAirgapped = {{ if (stat (joinPath .chezmoi.homeDir ".is_airgapped")) }}true{{ else }}false{{ end }}
